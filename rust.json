/*

This is a collection of common code snippets for various programming tasks and patterns. https://chatgpt.com/s/t_68df9213365881919b46bdeeb98c848d

🔹 1. Program Structure

boilerplate/skeleton→ basic executable program structure

Libary boilerplate / Libary skeleton → basic Libary/package structure

import / include → add library or dependency

comment → single line / multi-line comment

🔹 2. Variables & Data Types

var → declare variable

var-os → Read environment variable

const → declare constant

type → declare with type annotation (if supported)

assign → variable assignment

Convert → type conversion/casting

🔹 3. Input & Output

print → print to console

print format → print to console

input → read input from user

readfile → read from a file

writefile → write to a file

🔹 4. Control Flow

if → if statement

ifelse → if-else

switch/match → switch/case or match expression

for → for loop (counter-based)

foreach → loop over collection

while → while loop

break → break out of loop

continue → continue to next iteration

🔹 5. Functions

func → function definition


unit-test → function test definition

extention → Extend functions of a class

🔹 6. Data-Structures

array → array/list

array append → append to array/list

dict/map → dictionary, hash map

set → set

struct / class → basic class or struct

Json-read → parse/serialize JSON

Json-write → generate JSON from data

CSV-read → parse CSV file

🔹 7. Error-Handling

trycatch → try/catch block

throw / raise → throw an error

finally → cleanup section

🔹 8. Common Patterns

loopfile → loop through file lines

looparray → iterate over an array

loopdict → iterate over dictionary/map

main → entry point

🔹 9. Concurrent and parallelism

asyncfunc → async function

await → await promise/future

thread → threading/multithreading snippet

Stream -> stream data

class → class definition

inherit → class inheritance

🔹 10. Debugging & Utilities

hashpassword → securely hash a password

Logging_setup → Setup Logging for the app

decorator -> function/class decorator

🔹 11. Database

dbconnect → connect to a database

dbqueryrows → query multiple rows

dbqueryrow → query a single row

dbexec → execute insert/update/delete

dbtx → transaction with commit/rollback

dbclose → close database connection safely

dbping → check database health/connection

🔹 12. API–Server (exposing APIs)

apiserver → start an HTTP server

apiroute → define a route/endpoint handler

apiparsejson → parse request body JSON

apiresjson → send JSON response

apimiddleware → add middleware (logging, auth, CORS)

🔹 13. API–Client (consuming APIs)

apiclientget → make GET request to external API

apiclientpost → make POST request to external API

apiclientreq → generic HTTP request with headers

apiclientjson → parse JSON response into struct
*/
{
	"boilerplate/skeleton": {
		"prefix": "!start",
		"body": [
			"$BLOCK_COMMENT_START",
			"TIP This is a boilerplate/skeleton for a basic executable program structure.",
			"TIP Don't forget to:",
			"TIP 1. Setup logging",
			"TIP 2. be consistent with variable and function naming conventions",
			"TIP 3. Break code into seprate reusable functions",
			"TIP 4. Make modules as independent as possible so that replacing them is easy",
			"TIP 5. Before writing modules write tests for it and write code to pass the tests",
			"$BLOCK_COMMENT_END",
			"fn main() {",
			"\tprintln!(\"Hello, world!\");",
			"}",
		]
	},
	"var signed int": {
		"prefix": "!var signed int",
		"body": [
			"let mut ${1:variable_name}:${2|i8,i16,i64,i128|} = ${3:value};"
		]
	},
	"var unsigned int": {
		"prefix": "!var unsinged int",
		"body": [
			"let mut ${1:variable_name}:${2|u8,u16,u64,u128|} = ${3:value};"
		]
	},
	"var float": {
		"prefix": "!var float",
		"body": [
			"let mut ${1:variable_name}:${2|f32,f64|} = ${3:value};"
		]
	},
}